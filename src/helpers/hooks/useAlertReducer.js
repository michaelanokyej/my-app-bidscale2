import React, { useReducer } from 'react';


const useAlertReducer = (state, action) => {
  /* useMemo hook to return a value */
  // const id = useMemo(() => ++counter, []);

  // return (suffix) => `id${id}_${suffix}`
};

export default useAlertReducer;
